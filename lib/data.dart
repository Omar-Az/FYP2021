import 'package:flutter/material.dart';

Color primaryColor= Color(0xEFEFEF);
List<BoxShadow> customShadow=[
  BoxShadow(
    color:Colors.white.withOpacity(0.5),
    spreadRadius: -5,
    offset: Offset(-5,-5),
    blurRadius: 30),

  BoxShadow(
      color:Colors.grey.withOpacity(0.5),
      spreadRadius: -5,
      offset: Offset(-5,-5),
      blurRadius: 30),

];